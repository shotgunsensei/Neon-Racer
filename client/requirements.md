## Packages
canvas-confetti | For celebrations when high scores are achieved
@types/canvas-confetti | Type definitions for canvas-confetti

## Notes
The game uses HTML5 Canvas heavily. We assume 60fps monitor refresh rates but use delta time where possible to normalize movement.
Backend routes required: GET /api/scores and POST /api/scores.
